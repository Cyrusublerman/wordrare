# Web framework (for Railway deployment)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
gunicorn>=21.2.0
pydantic>=2.4.0

# Core dependencies
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Data processing (lightweight versions)
numpy>=1.24.0,<2.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# NLP and embeddings (CPU-only, lightweight)
nltk>=3.8.0

# PyTorch CPU-only (much smaller than GPU version)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<2.2.0
sentence-transformers>=2.2.0,<2.4.0

# Phonetics
pronouncing>=0.2.0

# Scientific computing (minimal versions)
scipy>=1.10.0,<1.12.0

# Network analysis (for concept graph)
networkx>=3.1

# API clients
wordnik-py3>=2.0.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.65.0
pyyaml>=6.0
